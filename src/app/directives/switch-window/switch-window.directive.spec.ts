import { SwitchWindowDirective } from './switch-window.directive';

describe('SwitchWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchWindowDirective();
    expect(directive).toBeTruthy();
  });
});
